services:
  power-desk-config-tool:
    build:
      context: .
      dockerfile: Dockerfile
    image: power-desk-config-tool:latest
    container_name: power-desk-config-tool
    ports:
      - "25086:25086"
    environment:
      - NODE_ENV=production
      - PORT=25086
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:25086/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    # 资源限制（可选，根据需要调整）
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 128M
    # 日志配置
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

# 可选：如果需要持久化数据或配置
# volumes:
#   config_data:
#     driver: local

networks:
  default:
    name: power-desk-network
