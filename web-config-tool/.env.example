# Power Desk 配置工具环境变量

# GitHub Personal Access Token (可选)
# 用于提高 GitHub API 请求限制
# 创建方法：GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)
# 权限：只需要 public_repo 权限（或者不选择任何权限，只用于提高限制）
GITHUB_TOKEN=

# 服务器端口 (默认: 25086)
PORT=25086

# 运行模式 (development/production)
NODE_ENV=production
